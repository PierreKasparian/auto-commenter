// "use client"
// import { createClient as createAdminClient } from "@supabase/supabase-js";

// import { generateComment } from "../api/generate-com/route";

// import { getProviderId } from "@/utils/unipile/queries";
// import { getSystemPrompt } from "../api/generate-com/libs";


const page = async () => {
//   const comment = await generateComment(`Bref, je suis développeur web.
// Être dev freelance, c’est coder… mais pas que.

// C’est gérer les deadlines, les specs floues, les bugs invisibles… et les “petites modifs” qui prennent 3 heures.

// → C’est passer 2h à chercher un bug à cause d’un point-virgule oublié ;

// → Expliquer pour la 5ème fois que “rajouter un bouton”, ce n’est pas juste un clic ;

// → Et voir ton client valider un projet… avant de tout remettre en question le lendemain.

// Mais c’est aussi :

// → Le kiff quand ton code marche du premier coup (rare mais magique) ;

// → La satisfaction de livrer un projet dont t’es fier ;

// → La liberté d’organiser ton temps (même si tu bosses parfois à 23h) ;

// → Et ce petit moment où tu te dis : “ok, j’ai choisi la bonne voie”.

// À tous les devs (freelance ou pas) :

// C’est quoi votre anecdote la plus “mais pourquoi je fais ce métier déjà ?” ?

// Balancez-la en commentaire qu’on rigole (ou qu’on pleure) ensemble !`, "Sv6WoXHCQT2yu9P1V0agoQ", `Vous êtes fondateur d’un projet, sans CTO ni équipe dev.
// Vous avez peut-être déjà une vision claire du produit… mais le passage à l’action vous bloque côté technique.

// Mon approche :
// - J'écoute votre besoin et on en tire un produit clair et priorisé.
// - Je vais droit au but. Prototypes, MVPs, itérations rapides : on construit ce qui compte rapidement.
// - Vous restez concentrés sur la vision du produit, je m'occupe du reste.

// J’ai développé plusieurs projets pour une multinationale, une association et des TPE via des partenaires tech.

// 👇 Vous avez un projet ? Parlons-en
// Prenez un appel ici → https://cal.com/pierre-kasparian`,"fr")
//   // const id = getSystemPrompt("cocou")
//   // console.log(id)
//   // const adminAuthClient = createAdminClient(
//   //   process.env.NEXT_PUBLIC_SUPABASE_URL!,
//   //   process.env.NEXT_SUPABASE_SERVICE_ROLE_KEY!,
//   //   {
//   //     auth: {
//   //       autoRefreshToken: false,
//   //       persistSession: false,
//   //     },
//   //   }
//   // ).auth.admin;  
//   // const { data, error } = await adminAuthClient.getUserById("3a356d74-7f95-41a6-be51-36ae79319e5c")
//   // console.log(error)
//   // console.log(data.user?.email)
//   console.log(comment)
  return <div className=""></div>;
};

export default page
